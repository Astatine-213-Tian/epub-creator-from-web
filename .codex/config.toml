# Project-local Codex defaults. Applies only when this repository is trusted.
default_permissions = ":danger-full-access"
approval_policy = "never"

[mcp_servers.vercel]
url = "https://mcp.vercel.com"
enabled = false
